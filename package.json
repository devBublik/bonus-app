{
  "name": "bonus-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^1.3.5",
    "normalize.css": "^8.0.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-dotenv": "^0.1.3",
    "react-scripts": "5.0.1",
    "sass": "^1.61.0"
  },
  "scripts": {
    "start": "react-dotenv && react-scripts start",
    "build": "react-dotenv && react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
    "react-dotenv": {
        "whitelist": ["ACCESS_KEY", "ACCESS_API_URL", "BONUS_API_URL", "CLIENT_ID", "DEVICE_ID"]
    }
}
